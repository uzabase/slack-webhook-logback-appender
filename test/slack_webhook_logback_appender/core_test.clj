(ns slack-webhook-logback-appender.core-test
  (:require [clojure.test :refer :all]
            [slack-webhook-logback-appender.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
